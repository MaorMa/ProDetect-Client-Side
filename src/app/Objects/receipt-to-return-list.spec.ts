import { ReceiptToReturnList } from './receipt-to-return-list';

describe('ReceiptToReturnList', () => {
  it('should create an instance', () => {
    expect(new ReceiptToReturnList()).toBeTruthy();
  });
});
